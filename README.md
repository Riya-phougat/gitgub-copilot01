# gitgub-copilot01

# Simple HTML Login Page 🔐

A clean, responsive login interface built with semantic HTML and custom CSS. This project was created to practice Git branching, SSH authentication, and basic web structure.

## 🚀 Features
* **Responsive Design:** Works on mobile, tablet, and desktop.
* **Semantic HTML:** Uses proper form labels and input types.
* **Modern CSS:** Features a centered layout with hover effects and shadows.
* **Form Validation:** Uses HTML5 `required` attributes for client-side validation.

## 🛠️ Technologies Used
* **HTML5:** For the page structure.
* **CSS3:** For styling and layout (Flexbox).
* **Git/GitHub:** For version control and deployment.

